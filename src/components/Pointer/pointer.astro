---
import PixelTrail from './PixelTrail.jsx';
---

<PixelTrail 
  client:load
  gridSize={40}
  trailSize={0.1}
  maxAge={250}
  interpolate={5}
  color="#ffffff"
  gooeyFilter={{ id: 'goo-filter', strength: 10 }}
/>
